---
nav-links:
#  - text: About
#    href: #
  - text: CV
    href: /cv/
  - text: Blog
    href: /
#  - text: Archives
#    href: #

social:
  - name: LinkedIn
    href: https://www.linkedin.com/in/devonwalker
    icon: <i class="fa fa-linkedin-square fa-2x"></i>
  - name: Twitter
    href: https://twitter.com/devwalk
    icon: <i class="fa fa-twitter-square fa-2x"></i>
  - name: GitHub
    href: https://github.com/devonwa
    icon: <i class="fa fa-github-square fa-2x"></i>
---

<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en-us">

    {% include head.html %}

    <body>

        <!-- Navigation -->
        <nav class="navbar navbar-default navbar-static-top" role="navigation">
            <div class="container">
            
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#Links">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="/">Devon Walker</a>
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="Links">
                  <ul class="nav navbar-nav">
                  {% for t in page.nav-links %}
                      {% if t.text == page.title %}
                        <li class="active">
                      {% else %}
                        <li>
                      {% endif %}
                        <a href="{{ t.href }}" title="{{ t.title }}">{{ t.text }}</a>
                      </li>
                  {% endfor %}
                  </ul>
                  <div class="navbar-text navbar-right">
                      {% for s in page.social %}
                          <a href="{{ s.href }}" alt="{{ s.name }}">{{ s.icon }}</a> &nbsp;
                      {% endfor %}
                  </div>
                </div>
            
            </div>
        </nav><!-- /Navigation -->

          <!-- Page Content -->
              <div class="container page-content">
                  {{ content }}
              </div>
          
    <!-- Javascript -->
    {% include javascript.html %}
    </body>
</html>
